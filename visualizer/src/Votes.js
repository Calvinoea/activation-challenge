import React from "react";

const Votes = () => {
    return ( <
        section className = "Votes" >
        <
        h1 > This is the Contact page < /h1> <
        /section>
    );
};

export default Votes;